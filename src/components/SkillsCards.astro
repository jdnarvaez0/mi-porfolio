---
const { name, url, img, title, alt } = Astro.props;
---

<div class="flex flex-col items-center rounded-lg p-4 transition-transform hover:scale-105">
	<a href={url} target="_blank" rel="noopener noreferrer">
		<img src={img} title={title} alt={alt} class="h-8 w-8 rounded-lg md:h-9 md:w-9" />
	</a>
	<p class="pt-1 text-sm md:text-[12px]">{name}</p>
</div>
